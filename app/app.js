
// setup redux and react

// do project state

// project json as argument

// get folder and build json by walking the folder structure

// it takes an already saved folder structure json, and compares to the new one
  // keeping the given one as preference for folder/file settings

// the component takes the state and builds the tree

// ask angus how we do styling
  // just use inline css and html for the container

// on click on the filenames output the json for the file (including full location)